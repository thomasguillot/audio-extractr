# Local working docs (specs, plans, scratch), never committed
/docs/*

# Release artifacts
/dist
/App/build

# Site build output and dependencies
/site/node_modules
/site/dist
/site/.astro

# Fetched tool binaries (scripts/fetch-binaries.sh)
/Support/binaries

# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Xcode (project is generated by xcodegen)
DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Local tooling directories (personal to my machine, never committed)
.*/
!.github/
!.well-known/
